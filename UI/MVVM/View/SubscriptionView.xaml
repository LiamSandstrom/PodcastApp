<UserControl x:Class="UI.MVVM.View.SubscriptionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.MVVM.View" xmlns:viewmodel="clr-namespace:UI.MVVM.ViewModel" d:DataContext="{d:DesignInstance Type=viewmodel:SubscriptionViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="center"
          VerticalAlignment="Center">

        <ScrollViewer 
                      VerticalScrollBarVisibility="Auto"
                      Padding="0,0,30,0"
                      MaxWidth="1300"
                      x:Name="MyScrollViewer"
        >

            <ItemsControl ItemsSource="{Binding Podcasts}"
                              SnapsToDevicePixels="True"
                          >
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#70f1f1f1"
                            BorderThickness="1,1,1,1"
                            Padding="10"
                            Margin="0,0,0,10"
                            Width="600"
                            Background="#0F0F0F"
                            CornerRadius="15"
                            Cursor="Hand"
                            >
                            <Border.InputBindings>
                                <MouseBinding MouseAction="LeftClick"
              Command="{Binding DataContext.PodcastClickCommand,
                                RelativeSource={RelativeSource AncestorType=UserControl}}"
              CommandParameter="{Binding RssUrl}" />
                            </Border.InputBindings>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition />
                                </Grid.RowDefinitions>

                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90" />
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <Border Grid.Column="0"
                                    Background="Beige"
                                    CornerRadius="20"
                                     >
                                    <Image Source="{Binding PodcastImgUrl}" />
                                </Border>
                                <TextBlock Grid.Column="1" Text="{Binding CustomName}"
                                               Foreground="White"
                                               FontWeight="SemiBold"
                                               FontSize="22"
                                               TextWrapping="Wrap"
                                               Margin="15,0,0,10"
                                               VerticalAlignment="Bottom"
                                               />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </ScrollViewer>

    </Grid>
</UserControl>
