<Window x:Class="UI.MVVM.View.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewmodel="clr-namespace:UI.MVVM.ViewModel"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=viewmodel:LoginViewModel, IsDesignTimeCreatable=True}"
        Title="Login"
        Height="300" Width="400"
        Background="#131313"
        WindowStyle="None"
        AllowsTransparency="True"
        WindowStartupLocation="CenterScreen"
        >

    <!-- Custom window chrome for rounded corners -->
    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="0"
                      ResizeBorderThickness="5"
                      CornerRadius="10"
                      GlassFrameThickness="0"/>
    </WindowChrome.WindowChrome>

    <Border>
        <Grid>
            <Grid.RowDefinitions>
                <!-- Topbar -->
                <RowDefinition Height="30"/>
                <!-- Content -->
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Top control / top bar -->
            <Border Grid.Row="0"
                    Background="#252525"
                    MouseDown="Border_MouseDown">
                <Grid>
                    <Label Content="PoddApp" VerticalAlignment="Center" Margin="10,0,0,0" Foreground="White" FontWeight="SemiBold"/>

                    <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                        <Button Width="20" Height="20" Content="🗕" FontSize="13" Background="Transparent" BorderThickness="0" Foreground="#e8e6e6" Click="ButtonMinimize_Click" Padding="0,0,0,3" Margin="0,0,8,0"/>
                        <Button Width="20" Height="20" Content="☐" FontSize="14" Background="Transparent" BorderThickness="0" Margin="0,0,8,0" Foreground="#e8e6e6" Click="ButtonMaximize_Click"/>
                        <Button Width="20" Height="20" Content="✕" FontSize="14" Margin="0,0,8,0" Background="Transparent" BorderThickness="0" Foreground="#e8e6e6" Click="ButtonClose_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Login form -->
            <Grid Grid.Row="1">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="250">
                    <TextBlock Text="Enter your email" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
                    <TextBox Text="{Binding Email}"   Height="35" FontSize="14" Background="#252525" Foreground="White" BorderThickness="1" BorderBrush="#9a9a9a" Padding="5"/>
                    <Button Content="Login" Command="{Binding LoginCommand}" Height="35" Margin="0,10,0,0" Background="#252525" Foreground="White" BorderThickness="1" BorderBrush="#9a9a9a"/>
                </StackPanel>
            </Grid>

        </Grid>
    </Border>
</Window>
