<UserControl x:Class="UI.MVVM.View.CategoriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:UI.MVVM.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid VerticalAlignment="Center"
          >
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="100"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="30"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>



        <Border Height="80" VerticalAlignment="center" Grid.Column="0" BorderBrush="White" BorderThickness="0,0,0,1">
            <TextBlock TextAlignment="Center" VerticalAlignment="Center" Grid.Column="2" TextWrapping="Wrap"  Text="Add Custom Category" Foreground="White" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,10"/>
        </Border>
        <StackPanel Grid.Row="1" Margin="0,10,0,0">
            <TextBlock  Text="Category name" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
            <TextBox  x:Name="MyTextBox" Text="{Binding Name}"   Height="35" FontSize="14" Background="#252525" Foreground="White" BorderThickness="1" BorderBrush="#9a9a9a" Padding="5" Margin="0,0,0,10"/>

        </StackPanel>
        <Button Grid.Row="3" Grid.Column="0" Command="{Binding AddCommand}" Height="40" Content="Add" Style="{StaticResource ModernButton}"/>



        <Border Height="80" VerticalAlignment="center" Grid.Column="2" BorderBrush="White" BorderThickness="0,0,0,1">
            <TextBlock TextAlignment="Center" VerticalAlignment="Center" Grid.Column="2" TextWrapping="Wrap"  Text="Rename Custom Category" Foreground="White" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,10"/>
        </Border>
        <StackPanel Grid.Column="2" Grid.Row="1" Margin="0,10,0,0">
            <TextBlock  Text="Category" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
            <ComboBox Grid.Column="2" Grid.Row="1"  Width="150" ItemsSource="{Binding Categories}"
          SelectedItem="{Binding SelectedCategoryRename, Mode=TwoWay}"
          DisplayMemberPath="Name"/>
        </StackPanel>
        <StackPanel Grid.Column="2" Grid.Row="2">
            <TextBlock  Text="New name" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
            <TextBox  Text="{Binding NewName}"   Height="35" FontSize="14" Background="#252525" Foreground="White" BorderThickness="1" BorderBrush="#9a9a9a" Padding="5" Margin="0,0,0,10"/>

        </StackPanel>
        <Button Grid.Row="3" Grid.Column="2" Command="{Binding RenameCommand}" Height="40" Content="Rename" Style="{StaticResource ModernButton}"/>




        <Border Height="80" VerticalAlignment="center" Grid.Column="4" BorderBrush="White" BorderThickness="0,0,0,1">
            <TextBlock TextAlignment="Center" VerticalAlignment="Center" Grid.Column="2" TextWrapping="Wrap"  Text="Remove Custom Category" Foreground="White" FontSize="24" FontWeight="SemiBold" Margin="0,0,0,10"/>
        </Border>
        <StackPanel Grid.Column="4" Grid.Row="1" Margin="0,10,0,0">
            <TextBlock  Text="Category" Foreground="White" FontSize="16" Margin="0,0,0,10"/>
            <ComboBox  Width="150" ItemsSource="{Binding Categories}"
          SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
          DisplayMemberPath="Name"
                       Foreground="Black"/>

        </StackPanel>
        <Button Grid.Row="3" Grid.Column="4" Command="{Binding RemoveCommand}" Height="40" Content="Remove" Style="{StaticResource ModernButton}"/>


    </Grid>
</UserControl>
